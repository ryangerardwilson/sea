#ifndef CONFIG_H
#define CONFIG_H

#define DEFAULT_PORT 3000
#define DB_PATH "./sea.db"
#define MAX_QUERY_PARAMS 32  // Tune this crap later

// Route definitions could go here, loaded at runtime
// But for now, hardcode in main.c like a caveman

#endif /* CONFIG_H */
